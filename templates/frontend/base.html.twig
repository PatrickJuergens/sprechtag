<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="utf-8">
    <title>BBS1-Leer - Terminvereinbarung Eltern- und Ausbildersprechtag</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <link rel="icon" type="image/png"  href="{{ asset('frontend/images/favicon.png') }}">
    <!-- CSS -->
    <link href="{{ asset('frontend/css/bootstrap.min.css') }}" rel="stylesheet" type="text/css" />
    <script src="{{ asset('frontend/js/jquery.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('frontend/js/bootstrap.min.js') }}" type="text/javascript"></script>
    {% block css_additional %}{% endblock %}
</head>
<body>
<section class="vh-100" style="background-color: #eee;">
    <div class="container h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-lg-12 col-xl-11">
                <div class="card text-black" style="border-radius: 25px;">
                    <div class="card-body p-md-5">
                        <div class="row justify-content-center">
                            <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">

                                {% for label, messages in app.flashes() %}
                                        {% for message in messages %}
                                            <div class="alert alert-{{ label }}" role="alert">
                                                {{ message }}
                                            </div>
                                        {% endfor %}
                                {% endfor %}

                                {% block content %}
                                {% endblock %}
                            </div>
                            <div class="col-md-10 col-lg-6 col-xl-7 text-center align-middle order-1 order-lg-2">

                                <a href="https://www.bbs1-leer.de/"><img class="img-fluid" src="{{ asset('frontend/images/logo_lg.png') }}"  alt="Logo der BBS 1 Leer"></a>
                                <p class="text-center h2 fw-bold mb-5 mx-1 mx-md-4 mt-4">Anmeldung zum Eltern- und Ausbildersprechtag</p>

                                <p class="text-center h5 mb-5 mx-1 mx-md-4 mt-4">am Donnerstag, 30.11.2023</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% block javascript_additional %}{% endblock %}
</body>
</html>