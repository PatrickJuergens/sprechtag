{% extends 'frontend/base.html.twig' %}

{% block content %}

    <h1>Hallo {{ teacher.firstName }} {{ teacher.lastName }}</h1>

    <form method="post">
        <label>Wann haben Sie Zeit, bitte wählen Sie aus, wann Sie verfügbar sind (entfernen Sie den Haken, wenn Sie NICHT verfügbar sind):</label>
        {% for timeFrame in timeFrames %}
            <input type="checkbox" name="timeFrame[{{timeFrame}}]" id="tf_{{ timeFrame.id }}" value="1" {% if teacher.getAvailableTimeFrames().contains(timeFrame)  %}checked{% endif %} class="flat" /> {{timeFrame}}
            <br/>
        {% endfor %}

        <button class="btn btn-primary btn-lg" type="submit">Verfügbarkeiten erfassen</button>
    </form>


{% endblock %}

