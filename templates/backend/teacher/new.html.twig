<!DOCTYPE html>

<title>New Teacher</title>

{% block body %}
    <h1>Create new Teacher</h1>

    {{ include('backend/teacher/_form.html.twig') }}

    <a href="{{ path('app_teacher_index') }}">back to list</a>
{% endblock %}
