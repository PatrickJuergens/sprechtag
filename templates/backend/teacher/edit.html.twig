<!DOCTYPE html>

<title>Edit Teacher</title>

{% block body %}
    <h1>Edit Teacher</h1>

    {{ include('backend/teacher/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_teacher_index') }}">back to list</a>

    {{ include('backend/teacher/_delete_form.html.twig') }}
{% endblock %}
